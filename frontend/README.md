# Frontend

Next.js Pages Router frontend for the MCP Customer Report Agent.

## Run

```bash
npm install
NEXT_PUBLIC_API_URL=http://localhost:8010 npm run dev
```

Open `http://localhost:3000`.

Voice input uses the browser Web Speech API, so use Chrome or Edge for the demo.
